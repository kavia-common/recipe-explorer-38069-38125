# Non-interactive, low-memory defaults for CI and containerized environments (no secrets here)
BROWSER=none
HOST=0.0.0.0
# Primary port hint; wrapper will auto-select a free one to avoid CRA prompts
PORT=3000
REACT_APP_PORT=3000
CI=true

# Disable polling to reduce CPU usage
CHOKIDAR_USEPOLLING=false
WATCHPACK_POLLING=false

# Keep dev server light
GENERATE_SOURCEMAP=false
FAST_REFRESH=false

# Cap node memory to avoid OOM (exit 137) in constrained environments
NODE_OPTIONS=--max-old-space-size=1024

# Optional health endpoint path for healthcheck.js
# REACT_APP_HEALTHCHECK_PATH=/
