# CI-friendly local development defaults (no secrets)
# Prevent CRA from prompting, reduce resource usage, and avoid OOM (137) in constrained envs.

# Bind and port
HOST=0.0.0.0
PORT=3000
REACT_APP_PORT=3000

# CI mode and non-interactive browser behavior
CI=true
BROWSER=none

# Reduce file-watcher overhead in containers/CI
CHOKIDAR_USEPOLLING=false
WATCHPACK_POLLING=false

# Keep builds lighter and faster in CI/dev
GENERATE_SOURCEMAP=false
FAST_REFRESH=false

# Cap Node memory to avoid OOMs leading to exit 137
NODE_OPTIONS=--max-old-space-size=1024

# Optional healthcheck path (CRA serves index at / by default)
# HEALTHCHECK_PATH=/
# REACT_APP_HEALTHCHECK_PATH=/
