# CI-friendly, non-interactive defaults for CRA dev server.
# These are safe to commit; no secrets here.

# Avoid opening a browser in CI/container
BROWSER=none

# Bind to all interfaces for container environments
HOST=0.0.0.0

# Default port; start wrapper will auto-pick a free one from here
PORT=3000
REACT_APP_PORT=3000

# CI mode to prevent watch prompts, etc.
CI=true

# Disable heavy polling to reduce CPU/IO
CHOKIDAR_USEPOLLING=false
WATCHPACK_POLLING=false

# Disable source maps to reduce memory/CPU
GENERATE_SOURCEMAP=false

# Disable fast refresh in CI-like environments
FAST_REFRESH=false

# Cap Node heap to avoid OOM (exit code 137)
NODE_OPTIONS=--max-old-space-size=1024

# Optional healthcheck path for dev server
REACT_APP_HEALTHCHECK_PATH=/
