# Committed CI/dev defaults (no secrets). These values make CRA start non-interactive and low-resource.
# Note: The orchestrator may override these via environment variables.
BROWSER=none
HOST=0.0.0.0
PORT=3000
REACT_APP_PORT=3000
CI=true

# Disable heavy polling in containers/CI
CHOKIDAR_USEPOLLING=false
WATCHPACK_POLLING=false

# Keep builds lean for dev server
GENERATE_SOURCEMAP=false
FAST_REFRESH=false

# Cap Node memory to avoid OOM (exit 137) in constrained CI runners
NODE_OPTIONS=--max-old-space-size=1024

# Optional healthcheck path (root by default)
# REACT_APP_HEALTHCHECK_PATH=/
