# This file is intentionally committed for CI/dev to reduce memory usage and prevent watcher CPU spikes.
# Do not add secrets here.

# Prevent CRA from generating large source maps in dev (saves memory in CI)
GENERATE_SOURCEMAP=false

# Disable polling watcher in CI-like environments to reduce CPU and memory
# CRA uses CHOKIDAR_USEPOLLING; set to false to avoid heavy polling
CHOKIDAR_USEPOLLING=false

# Limit node memory usage to avoid OOM kills (exit code 137)
# This affects Node child processes spawned by react-scripts.
NODE_OPTIONS=--max-old-space-size=1024
