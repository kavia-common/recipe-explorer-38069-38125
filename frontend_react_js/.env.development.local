# This file contains non-secret defaults to run CRA dev server in CI with low resource usage.
# Do not commit secrets. Orchestrator will inject real .env when needed.

# Avoid opening browser and interactive prompts
BROWSER=none
CI=true

# Bind to all interfaces for container usage and set default port
HOST=0.0.0.0
PORT=3000
REACT_APP_PORT=3000

# Reduce resource usage to avoid exit 137 (SIGKILL) in constrained CI containers
GENERATE_SOURCEMAP=false
CHOKIDAR_USEPOLLING=false
WATCHPACK_POLLING=false
FAST_REFRESH=false

# Cap node memory
# Note: can be overridden by orchestrator via env injection
NODE_OPTIONS=--max-old-space-size=1024

# Silence CRA/webpack noisy output and outdated browserslist prompt in CI
# Update browserslist DB in postinstall; this suppresses runtime warnings
# (No env var needed for browserslist; see package.json postinstall)
