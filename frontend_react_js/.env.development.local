# Non-interactive, CI-friendly defaults to stabilize dev server in constrained environments.
# These are safe defaults for development and CI. Do not put secrets here.

# Prevent CRA from opening a browser and ensure container binding
BROWSER=none
HOST=0.0.0.0

# Default port; start-noninteractive.js will auto-pick a free one if needed
PORT=3000
REACT_APP_PORT=3000

# CI flags to avoid watch mode interactivity and heavy features
CI=true
FAST_REFRESH=false

# Reduce resource usage to avoid OOM (137)
NODE_OPTIONS=--max-old-space-size=1024
GENERATE_SOURCEMAP=false

# Disable heavy polling watchers
CHOKIDAR_USEPOLLING=false
WATCHPACK_POLLING=false

# Optional readiness path
REACT_APP_HEALTHCHECK_PATH=/
