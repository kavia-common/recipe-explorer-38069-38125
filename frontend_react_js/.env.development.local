# Non-interactive, low-resource dev settings for CI and containers.
# No secrets here; safe to commit per ENVIRONMENT.md guidance.

# Prevent CRA from launching a browser
BROWSER=none

# Bind to all interfaces in container
HOST=0.0.0.0

# Default port (start-noninteractive auto-selects free port if needed)
PORT=3000

# CI-like behavior; prevents watch-mode assumptions
CI=true

# Reduce file watching overhead (avoid polling to save CPU/memory)
CHOKIDAR_USEPOLLING=false
WATCHPACK_POLLING=false

# Disable fast refresh overhead in CI
FAST_REFRESH=false

# Avoid generating sourcemaps to reduce memory footprint
GENERATE_SOURCEMAP=false

# Cap node memory to avoid OOM -> SIGKILL (137)
NODE_OPTIONS=--max-old-space-size=1024
