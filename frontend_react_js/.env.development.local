# Committed CI-friendly defaults for non-interactive dev server runs
# These are safe for CI and local dev; no secrets included.

# Non-interactive browser/host/port
BROWSER=none
HOST=0.0.0.0
PORT=3000
REACT_APP_PORT=3000
CI=true

# Reduce resource usage to avoid OOM (exit 137) and heavy file watching
CHOKIDAR_USEPOLLING=false
WATCHPACK_POLLING=false
GENERATE_SOURCEMAP=false
FAST_REFRESH=false

# Cap Node heap to reduce memory footprint in constrained CI environments
NODE_OPTIONS=--max-old-space-size=1024

# Optional healthcheck path for healthcheck.js script
REACT_APP_HEALTHCHECK_PATH=/
