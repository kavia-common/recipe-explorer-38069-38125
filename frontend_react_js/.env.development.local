# CI-friendly, low-memory, non-interactive defaults for CRA dev server
# These variables are safe to commit (no secrets) and help stabilize CI and containers.

# Do not auto-open browser
BROWSER=none

# Bind to all interfaces for container access
HOST=0.0.0.0

# Default port; start-noninteractive.js will choose a free port near this
PORT=3000
REACT_APP_PORT=3000

# CI non-interactive mode
CI=true

# Reduce resource usage to avoid exit 137 (OOM) in constrained environments
CHOKIDAR_USEPOLLING=false
WATCHPACK_POLLING=false
GENERATE_SOURCEMAP=false
FAST_REFRESH=false

# Cap Node memory usage (in MB)
NODE_OPTIONS=--max-old-space-size=1024

# Optional healthcheck path for external probes (falls back to '/')
REACT_APP_HEALTHCHECK_PATH=/

# Optional: add app envs noted by container metadata if needed at runtime
REACT_APP_API_BASE=
REACT_APP_BACKEND_URL=
REACT_APP_FRONTEND_URL=
REACT_APP_WS_URL=
REACT_APP_NODE_ENV=development
REACT_APP_NEXT_TELEMETRY_DISABLED=1
REACT_APP_ENABLE_SOURCE_MAPS=false
REACT_APP_TRUST_PROXY=false
REACT_APP_LOG_LEVEL=info
REACT_APP_FEATURE_FLAGS=
REACT_APP_EXPERIMENTS_ENABLED=false
