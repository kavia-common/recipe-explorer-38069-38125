# Non-interactive, low-resource defaults for dev server in CI-like environments.
# No secrets should be committed here.

# Prevent CRA from opening a browser and ensure container-friendly binding
BROWSER=none
HOST=0.0.0.0

# Default ports; wrapper may select a nearby free port automatically
PORT=3000
REACT_APP_PORT=3000

# CI mode disables interactive prompts
CI=true

# Disable heavy file-watcher polling; rely on native fs events
CHOKIDAR_USEPOLLING=false
WATCHPACK_POLLING=false

# Reduce memory/CPU by disabling sourcemaps and fast refresh
GENERATE_SOURCEMAP=false
FAST_REFRESH=false

# Cap Node memory to prevent OOM (exit 137) in constrained environments
NODE_OPTIONS=--max-old-space-size=1024

# Optional healthcheck path for healthcheck.js
REACT_APP_HEALTHCHECK_PATH=/
