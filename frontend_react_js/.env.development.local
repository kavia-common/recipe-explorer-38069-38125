# CI-friendly defaults for non-interactive CRA dev server
# These values contain no secrets and are safe to commit.

# Bindings and port
BROWSER=none
HOST=0.0.0.0
PORT=3000
REACT_APP_PORT=3000

# CI and behavior flags
CI=true
CHOKIDAR_USEPOLLING=false
WATCHPACK_POLLING=false
FAST_REFRESH=false

# Build flags
GENERATE_SOURCEMAP=false

# Cap Node memory to reduce risk of OOM (exit 137)
NODE_OPTIONS=--max-old-space-size=1024

# Optional healthcheck path for http get checks (defaults to "/")
#REACT_APP_HEALTHCHECK_PATH=/
