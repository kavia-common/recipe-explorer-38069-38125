# CI-friendly defaults for development to reduce memory and avoid interactive prompts.
# These values are safe and contain no secrets.

# Disable heavy file watching in containers/CI
CHOKIDAR_USEPOLLING=false
WATCHPACK_POLLING=false

# Disable fast refresh to lower memory/CPU in CI
FAST_REFRESH=false

# Disable source maps to reduce memory footprint
GENERATE_SOURCEMAP=false

# Cap Node memory to avoid OOM causing exit code 137 under constrained CI environments
# start-noninteractive.js also sets this if unset; keeping here as an extra safeguard
NODE_OPTIONS=--max-old-space-size=1024

# Ensure CRA does not attempt to open a browser
BROWSER=none

# Bind to all interfaces for containerized environments
HOST=0.0.0.0

# Default port; can be overridden by orchestrator via REACT_APP_PORT/PORT
PORT=3000
