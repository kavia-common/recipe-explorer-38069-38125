# Non-interactive, CI-friendly defaults for CRA dev server
# These are safe to commit as they contain no secrets.

# Do not auto-open browser
BROWSER=none
# Bind to all interfaces in container
HOST=0.0.0.0
# Default port; wrapper will auto-pick a free nearby port to avoid prompts
PORT=3000
REACT_APP_PORT=3000

# CI behavior and watch settings
CI=true
CHOKIDAR_USEPOLLING=false
WATCHPACK_POLLING=false
FAST_REFRESH=false

# Lower memory/CPU usage
GENERATE_SOURCEMAP=false
# Cap Node memory to avoid exit 137 OOM in constrained environments
NODE_OPTIONS=--max-old-space-size=1024

# Optional: healthcheck path for healthcheck.js
REACT_APP_HEALTHCHECK_PATH=/
