# CI-friendly local development defaults to reduce resource usage.
# These are safe, non-secret defaults used by Create React App dev server.

# Networking
HOST=0.0.0.0
PORT=3000

# Performance and memory
GENERATE_SOURCEMAP=false
CHOKIDAR_USEPOLLING=false
WATCHPACK_POLLING=false
FAST_REFRESH=false

# Cap node memory to avoid OOM kills in constrained CI
# Note: start-noninteractive.js also enforces this if not set.
NODE_OPTIONS=--max-old-space-size=1024

# React / CRA
BROWSER=none
CI=true
