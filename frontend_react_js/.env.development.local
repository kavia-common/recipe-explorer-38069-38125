# Reduce memory and file watching overhead in constrained CI environments
GENERATE_SOURCEMAP=false
CHOKIDAR_USEPOLLING=false
WATCHPACK_POLLING=false
# Limit node memory usage footprint (react-scripts respects Node's defaults but CI may honor this)
# Note: Kavia orchestrator should set environment variables from container config as needed.
