# Non-interactive, CI-friendly defaults for Create React App dev server
# These are safe defaults without secrets; adjust via orchestrator as needed.

# Bind and port
HOST=0.0.0.0
PORT=3000
REACT_APP_PORT=3000

# Non-interactive and headless
BROWSER=none
CI=true

# Reduce resource use to avoid OOM (exit 137)
NODE_OPTIONS=--max-old-space-size=1024

# Disable heavy file watcher polling
CHOKIDAR_USEPOLLING=false
WATCHPACK_POLLING=false

# Avoid slow source map generation in CI
GENERATE_SOURCEMAP=false

# Disable fast refresh overhead in CI
FAST_REFRESH=false

# Healthcheck path for CI (optional)
REACT_APP_HEALTHCHECK_PATH=/
