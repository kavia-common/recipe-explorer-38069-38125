# Non-interactive, low-memory defaults for CI and development
# These values are safe to commit (no secrets).
BROWSER=none
HOST=0.0.0.0
PORT=3000
REACT_APP_PORT=3000
CI=true

# Disable heavy file watcher polling (uses efficient FS notifications instead)
CHOKIDAR_USEPOLLING=false
WATCHPACK_POLLING=false

# Reduce memory and build overhead
GENERATE_SOURCEMAP=false
FAST_REFRESH=false

# Cap Node heap to avoid OOM (exit code 137) in constrained environments
NODE_OPTIONS=--max-old-space-size=1024

# Optional health path used by healthcheck.js
REACT_APP_HEALTHCHECK_PATH=/
