# CI-friendly defaults for React dev server
# Ensures non-interactive mode, reduced memory/CPU footprint, and stable behavior in containers.

# Do not open a browser and bind to all interfaces
BROWSER=none
HOST=0.0.0.0

# Default port; start-noninteractive.js will auto-find a free port near this if needed
PORT=3000

# Continuous Integration environment
CI=true

# Reduce watcher overhead in CI
CHOKIDAR_USEPOLLING=false
WATCHPACK_POLLING=false

# Keep builds light
GENERATE_SOURCEMAP=false

# Disable Fast Refresh in CI-like environments
FAST_REFRESH=false

# Cap Node memory to reduce risk of OOM (exit 137)
NODE_OPTIONS=--max-old-space-size=1024

# Optional: expose a readiness check path if a proxy is used; the internal script uses HEALTHCHECK_PORT if set.
# REACT_APP_HEALTHCHECK_PATH=/

# Optional: app port mirrored into REACT_APP_ prefix for consistency
REACT_APP_PORT=3000
