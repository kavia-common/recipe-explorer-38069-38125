# Non-interactive, low-memory defaults for CRA dev server in CI and containers
# This file is safe to commit (no secrets) and helps avoid false negatives from OOM (137) or interactive prompts.

# Networking / behavior
BROWSER=none
HOST=0.0.0.0
PORT=3000
REACT_APP_PORT=3000
CI=true

# Watcher / performance
CHOKIDAR_USEPOLLING=false
WATCHPACK_POLLING=false
GENERATE_SOURCEMAP=false
FAST_REFRESH=false

# Memory cap to protect against OOM in constrained CI environments
NODE_OPTIONS=--max-old-space-size=1024

# Optional readiness path used by healthcheck.js (falls back to "/")
# REACT_APP_HEALTHCHECK_PATH=/
