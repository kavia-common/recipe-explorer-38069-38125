# CI-friendly defaults for CRA dev server to avoid OOM and interactive prompts
# Note: This file contains no secrets and is safe to commit.

# Non-interactive, container-friendly
BROWSER=none
HOST=0.0.0.0
PORT=3000
CI=true

# Disable heavy file watcher polling
CHOKIDAR_USEPOLLING=false
WATCHPACK_POLLING=false

# Reduce memory / CPU footprint
GENERATE_SOURCEMAP=false
FAST_REFRESH=false
# Cap Node memory to avoid OOM -> exit code 137
NODE_OPTIONS=--max-old-space-size=1024

# Optional: align app port
REACT_APP_PORT=3000
